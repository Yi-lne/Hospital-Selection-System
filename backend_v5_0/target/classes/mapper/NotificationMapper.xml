<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chen.HospitalSelection.mapper.NotificationMapper">

    <resultMap id="BaseResultMap" type="com.chen.HospitalSelection.model.Notification">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="type" property="type"/>
        <result column="related_id" property="relatedId"/>
        <result column="content" property="content"/>
        <result column="is_read" property="isRead"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, type, related_id, content, is_read, create_time
    </sql>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user_notification
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <select id="selectUnreadByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user_notification
        WHERE user_id = #{userId}
        AND is_read = 0
        ORDER BY create_time DESC
    </select>

    <select id="countUnread" resultType="int">
        SELECT COUNT(*)
        FROM user_notification
        WHERE user_id = #{userId}
        AND is_read = 0
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user_notification
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.chen.HospitalSelection.model.Notification">
        INSERT INTO user_notification (user_id, type, related_id, content, is_read, create_time)
        VALUES (#{userId}, #{type}, #{relatedId}, #{content}, #{isRead}, #{createTime})
    </insert>

    <update id="markAsRead">
        UPDATE user_notification
        SET is_read = 1
        WHERE id = #{id}
    </update>

    <update id="batchMarkAsRead">
        UPDATE user_notification
        SET is_read = 1
        WHERE user_id = #{userId}
        AND is_read = 0
    </update>

    <delete id="deleteById">
        DELETE FROM user_notification
        WHERE id = #{id}
    </delete>

</mapper>
